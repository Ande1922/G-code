# G-code 项目级开发指南

## 项目愿景与学习目标

本项目以开源项目 `ai-agent-book` 的**实际章节结构和技术路线**为参考，构建一个从简到繁、层层递进的 Coding Agent。项目优先服务于学习和验证：代码应让使用者能够观察、理解并亲手实践 Agent 的底层机制，而不是尽早得到一个功能繁多但难以解释的成品。

核心学习目标包括：

- LLM 调用与消息模型；
- System Prompt、上下文和对话历史管理；
- Tool Calling 与工具协议；
- Agent Loop 中的观察、推理、行动和反馈；
- 文件读取、代码搜索、文件修改及命令执行；
- 工作区权限、安全边界与人工确认；
- 任务规划、状态管理、错误恢复和循环终止；
- 短期记忆、长期记忆及上下文压缩；
- 多 Agent 协作、任务委派和结果汇总；
- Coding Agent 的测试、评估、追踪与可观测性。

## 上游参考核验

根据 `ai-agent-book` 的结构逐步推进迭代。在开始任何具体实现前，必须访问并核对该项目当时的实际目录、章节内容、技术路线和许可证，并在本次迭代文档中记录所参考的仓库地址、版本或提交。不得仅凭项目名称、记忆或二手描述假定其内容，也不得复制许可证不兼容的代码或材料。

若上游结构已经变化，应说明变化及本项目采用的映射关系；若无法完成核验，应停止依赖上游细节，并明确标注阻塞或假设，而不是将推测写成事实。

## 仓库与目标分支

- 本项目的 GitHub 仓库地址为 `https://github.com/Ande1922/G-code.git`，本地远程名称统一使用 `origin`。
- 完成并验证迭代后，将提交推送到远程 `main` 分支；不得因为本地临时分支名称不同而把最终提交遗留在其他远程分支。
- 推送前必须先获取 `origin/main` 并检查提交历史，只允许快进或经过明确处理的正常集成；禁止为了绕过分歧而直接强制推送。
- 仓库地址中不得嵌入访问令牌或其他凭据。认证信息必须由受保护的环境配置提供，不得写入仓库、日志或文档。

## 单次迭代的强制流程

每次只推进**一个可验证的增量**。每次迭代必须按以下顺序完成并留下相应文档或记录：

1. **原理讲解**：结合已核验的对应章节，详细说明本次涉及的概念、数据流、关键算法、设计权衡、安全风险及替代方案。
2. **最小实现**：只引入验证当前概念所必需的代码，避免过早加入框架、通用层、复杂抽象或后续迭代才需要的能力。
3. **静态与动态验证**：为核心行为补充测试，运行适用的静态检查和动态测试，并说明它们覆盖的 Agent 行为、失败路径和边界情况。
4. **代码反解**：迭代完成后引用实际模块路径、关键类和函数，以具体输入、逐步执行过程和输出，解释本次原理如何落实到代码中。
5. **迭代总结**：列出本次新增能力、仍存在的限制、已知风险，以及下一阶段如何在当前实现上增量演进。

不得跳过原理讲解直接编码，也不得在实现完成后省略基于实际代码的示例说明。如果一个增量无法在一次迭代中完整讲解、实现和验证，应先将其进一步拆小。

## 建议迭代路线

- **Iteration 0**：项目骨架、模型接口和可替换的假模型。
- **Iteration 1**：单轮命令行问答 Agent。
- **Iteration 2**：多轮消息历史与上下文管理。
- **Iteration 3**：单工具调用和最小 Agent Loop。
- **Iteration 4**：文件读取、目录遍历与代码搜索工具。
- **Iteration 5**：受控文件编辑、diff 展示和用户确认。
- **Iteration 6**：命令执行、超时、退出码和权限控制。
- **Iteration 7**：任务规划、步骤状态、重试和终止策略。
- **Iteration 8**：记忆、摘要和上下文窗口治理。
- **Iteration 9**：多 Agent 分工与协作。
- **Iteration 10**：评估集、轨迹记录、可观测性及安全加固。

原则上按上述顺序推进。若确需调整顺序，必须先记录原因、依赖变化、学习影响和新的验证方式；不得以调整为由把多个能力合并进同一不可独立验证的迭代。

## 事实、推断与执行结果

所有界面、日志、轨迹、示例和文档必须明确区分以下三类信息：

1. **模型原始输出**：LLM 返回的内容或工具调用意图，仅代表模型生成结果；
2. **应用层推断**：程序对模型输出进行解析、校验、归纳或状态转换所得的结论；
3. **工具真实执行结果**：工具实际运行后返回的标准输出、标准错误、退出码、文件变化或结构化结果。

不得把模型生成的推理文本、计划、成功声明或模拟结果当成可验证事实。事实性结论必须能够追溯到输入、应用层处理或真实工具结果；展示时应使用清晰的类型、字段或标签保留来源。测试应覆盖三者发生冲突、格式错误或执行失败的情况。

## 工具安全与权限边界

所有高风险工具必须遵循最小权限原则，并采用默认拒绝、显式授权和结果可审计的设计。至少满足以下约束：

- 文件读写默认限制在明确的工作区根目录内；规范化并校验路径，防止 `..`、符号链接或其他方式越界。
- 写文件前展示目标和 diff；需要人工确认的阶段必须先确认再执行，不得把模型的同意当作用户确认。
- 删除、覆盖、批量替换等破坏性操作必须设置更严格的范围、确认和恢复机制。
- 命令执行使用允许列表或明确策略，限制工作目录、参数、环境变量、运行时长和输出大小，并保留退出码、标准输出与标准错误。
- 访问网络、凭据、系统目录或任何工作区外资源必须有明确边界和单独授权；敏感信息不得进入提示词、日志或轨迹。
- 工具调用在执行前必须经过结构校验和权限检查；超时、拒绝、部分成功与失败都应成为显式状态并反馈给 Agent Loop。
- 每次引入高风险能力时都要包含滥用场景、边界测试、人工确认点和安全替代方案。

## 统一示例任务

各迭代必须保留并复用一组统一的示例任务，以便学习者对照输入、轨迹、工具使用和结果，观察 Agent 能力如何逐步增强。除迭代专属示例外，至少持续维护以下基准场景：

> 阅读工作区中的一个小型示例项目，找到指定行为的实现位置，解释其工作方式，进行一处受控修改，并运行相关验证。

早期迭代可以只完成该任务中当前能力允许的部分，并明确说明未完成项；后续迭代应在不悄然改变任务目标和验收标准的前提下逐步扩展完成度。每次记录固定输入、预期能力、实际输出、失败边界和与上一迭代的差异。

## 完成标准

一次迭代只有同时满足以下条件才算完成：

- 上游参考已核验，且原理讲解先于实现；
- 改动保持最小并只对应一个可验证增量；
- 核心成功路径、失败路径和安全边界有静态或动态验证；
- 文档基于实际代码完成代码反解，并明确区分模型输出、应用推断和工具执行结果；
- 统一示例任务已运行或已说明当前阶段为何尚不能运行；
- 总结完整记录新增能力、限制、风险和下一步。
